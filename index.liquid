---
title: Homepage of my blog
layout: 'base.html'
---

<h1>This is my blog's homepage</h1>


<h2>Blog posts</h2>

{% for post in collections.posts %}
    <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
    <p>{{ post.data.description }}</p>
{% endfor %}